# Appium Configuration
appium.server.url=http://localhost:4723
appium.server.port=4723

# App Configuration
# Place your APK file in the 'apps' folder at the project root
# Update the APK filename in AppiumDriverManager.java (line 33)
app.path=apps/your-app.apk

# Device Configuration
platform.name=Android
automation.name=UiAutomator2
device.name=Android Device
# device.udid=  # Optional: specify device UDID

# App Package and Activity (if app is already installed)
# app.package=com.example.package
# app.activity=com.example.package.MainActivity

# Timeouts
implicit.wait=10
explicit.wait=10

# Reset Options
no.reset=false
full.reset=false

